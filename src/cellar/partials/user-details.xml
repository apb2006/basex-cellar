<div data-ng-init="setWindowTitle('User')" >
<ul class="breadcrumb">
   <li><a href="#/users">users</a> <span class="divider">/</span></li>
    <li class="active">{{user.name}}</li>
 </ul>

<img class="media-object img-circle" data-ng-src="{{user.avatar | userPic}}" />
      
<div>Logins: {{ user.logins }} 
last login:{{ user.last | fromNow}} 
joined:{{ user.created | fromNow}}
</div>

</div>